function [ floor_level ] = detect_background( background_arr )
floor_level = 0;
[row, column] = size(background_arr);
row_sum = sum(background_arr,2);
greatest_diff = 0;
for c=1:column-
    row_sum(c) = sum(background_arr)
end

end

